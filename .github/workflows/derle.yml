name: Kebap-Derleme
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: DLL-Yap
        run: g++ -shared -o KebapMenu.dll main.cpp -luser32
      - name: Dosyayi-Hazırla
        uses: actions/upload-artifact@v3
        with:
          name: Kebap-DLL
          path: KebapMenu.dll
